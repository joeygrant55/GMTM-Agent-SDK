# SPARQ agents module reserved for future agent code.
# Legacy Claude Agent SDK agents were removed (still reachable via git log on main).
# The live agent lives in backend/agent_api.py (Claude Sonnet 4.6 + web_search + query_database).

__all__: list[str] = []
